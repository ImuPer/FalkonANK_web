{% extends 'base.html.twig' %}

{% block title %}Edit delivery address
{% endblock %}

{% block body %}
	<div
		class="container my-5">

		<!-- Header -->
		<div class="d-flex justify-content-between align-items-center mb-4">
			<h1 class="h3 mb-0">
				✏️ Edit delivery address
			</h1>
			<span class="badge bg-dark">
				{{ delivery.deliveryStatus }}
			</span>
		</div>

		<!-- Card Form -->
		<div class="card shadow-sm">
			<div class="card-body">

				{{ form_start(form) }}
                    <div class="card shadow-sm border-0">
                        <div class="card-body">
                            <h6 class="mb-3 text-muted">📍 Full adress :</h6>

                            {{ form_widget(form.fullAddress, {
                                attr: {
                                    class: 'form-control',
                                    rows: 3
                                }
                            }) }}
                        </div>
                    </div>

                    <div class="d-flex justify-content-end mt-3">
                        <button type="submit" class="btn btn-dark">
                            💾 Save address
                        </button>
                    </div>
                {{ form_end(form) }}


			</div>
		</div>

		<!-- Back Link -->
		<div class="d-flex flex-wrap gap-2 mt-4">
			<a href="{{ path('app_delivery_show', {'id': delivery.id}) }}" class="btn btn-outline-secondary">
				← Back to deliveries
			</a>
		</div>

	</div>
{% endblock %}
